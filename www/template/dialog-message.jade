material-dialog.width-md(id="dialog-message", ng-if='textId == 0')
  .dialog-content
    h3.text-center(ng-switch="textId")
      span(ng-switch-when="0") 登录验证失败

  .dialog-actions(layout="horizontal" layout-align="space-around")
    material-button.material-button-raised.material-theme-light(flex="100" ng-click="clickDismiss()")
      div(ng-switch="textId")
        span(ng-switch-when="0") 重新登录
material-dialog.width-md(id="dialog-message", ng-if='textId != 0')
  .dialog-content
    h3.text-center(ng-switch="textId")
      span(ng-switch-when="1") 请稍等亲, 餐厅正在处理您上次提交的点菜
      span(ng-switch-when="2") 餐厅服务员将前来为您服务
      span(ng-switch-when="3") 菜品已成功下单！请耐心等待 ^_^

  .dialog-actions(layout="horizontal" layout-align="space-around")
    material-button.material-button-raised.material-theme-light(flex="100")
      div(ng-switch="textId")
        span(ng-switch-when="1") {{leftTime}}秒后，窗口将自动关闭。
        span(ng-switch-when="3") {{leftTime}}秒后，窗口将自动关闭。
        span(ng-switch-when="2") {{leftTime}}秒后，窗口将自动关闭。
